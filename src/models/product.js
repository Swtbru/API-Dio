// src/models/product.js

// Array para armazenar produtos em memória (simulação de banco de dados)
let produtos = [];

// Exportamos o array para usar em outras partes da aplicação
module.exports = produtos;
